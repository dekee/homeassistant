apiVersion: v1
kind: Service
metadata:
  name: music-assistant
  namespace: homeassistant
  labels:
    app: music-assistant
spec:
  type: ClusterIP
  selector:
    app: music-assistant
  ports:
    - name: http
      port: 8095
      targetPort: 8095
